services:
  web:
    build: .
    container_name: DEW_web
    ports:
      - "8080:80" # localhost:8080
    volumes:
      # mapez folderul curent in serverul apache
      - ./:/var/www/html
    environment:
      - APACHE_DOCUMENT_ROOT=/var/www/html

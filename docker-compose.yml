services:
  web:
    build: .
    container_name: DEW_web
    ports:
      - "8080:80"
    volumes:
      - ./:/var/www/html
    environment:
      - APACHE_DOCUMENT_ROOT=/var/www/html
    command: >
      sh -c "composer install --no-dev --working-dir=/var/www/html &&
             apache2-foreground"